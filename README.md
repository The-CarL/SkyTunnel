# SkyTunnel
A self-hosted, multi-protocol tunneling toolkit for resilient connectivity in restricted network environments. Deploys a pre-configured EC2 instance with DNS (iodine), ICMP (hans), and HTTPS (Chisel) tunnels — all managed via systemd with a single CloudFormation or Terraform deploy.
